#!/usr/bin/env groovy

pipeline{
    agent any
    stages{
        stage("Code Checkout"){
            steps{
                checkout scm
                echo "done"
            }
        }
        stage("Test"){
            steps{
                echo "done"
            }
        }
        stage("Build"){
            steps{
                echo "done"
            }
        }
    }    
}
